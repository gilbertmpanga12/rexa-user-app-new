import 'package:flutter/material.dart';

class SaloonProfile extends StatelessWidget{
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(title: Text('Complete Booking')),// ,backgroundColor: Color,
    );
  }
}